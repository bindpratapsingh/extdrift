# extdrift dependencies.
#
# The ANALYSIS half (unpack, features, scoring, report, CLI, tests) is pure standard
# library and needs nothing here - it runs on the 4 GB laptop as-is.
#
# The CAPTURE half (engine/sandbox) needs a browser driver:
#   * Chromium engine (engine/sandbox/capture.py): playwright + a Chromium build
#       pip install -r requirements.txt && playwright install chromium
#   * Firefox engine (engine/sandbox/firefox_capture.py): selenium + a Firefox binary
#       pip install -r requirements.txt   # then install Firefox; Selenium Manager
#       fetches geckodriver automatically. Turns real AMO .xpi pairs into behavioural rows.
playwright>=1.40
selenium>=4.15

# The DATA & ML tier (engine/synth, engine/batch, engine/ml) needs:
#     pip install -r requirements.txt
# These are light enough to train on the laptop; heavy browser capture wants the MacBook.
scikit-learn>=1.3
xgboost>=2.0
pandas>=2.0
numpy>=1.24
joblib>=1.3

# Optional: SHAP gives per-prediction attribution. We currently use scikit-learn's
# permutation_importance instead (lighter to install); enable SHAP if you want it.
# shap>=0.44
